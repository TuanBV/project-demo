# Copy to .env (repo root, gitignored) to override the docker-compose MySQL
# container credentials. These must match MYSQL_PASSWORD / MYSQL_DB in api/.env,
# or the fastapi container will fail to authenticate against MySQL.
MYSQL_PASSWORD=changeme
MYSQL_DB=nike
