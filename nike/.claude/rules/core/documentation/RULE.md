# Documentation Rules
- Update README or domain docs in the same slice as behavior changes.
- Document setup commands that were actually run from a clean state.
- Record schema decisions in `docs/decisions/` when they affect multiple modules.
- Keep examples secret-free and use explicit placeholders.
