# Dependency Rules
- Add a dependency only when the standard library or existing stack is insufficient.
- Pin Python application dependencies and preserve reproducible Node lockfiles.
- Do not run force-upgrade commands blindly; inspect advisories and breaking changes.
- Dependency upgrades require build/test evidence and a short migration note.
