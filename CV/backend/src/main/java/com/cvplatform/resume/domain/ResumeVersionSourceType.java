package com.cvplatform.resume.domain;

public enum ResumeVersionSourceType {
    PDF,
    /** Phase 4: an immutable snapshot of a STRUCTURED resume's sections at publish time. */
    STRUCTURED_SNAPSHOT
}
